Skills are a pool: start with `sdlc-workflow:{{PROC}}`, then load whichever procedure defines "good" for the files on disk (prd-gwt, design-contract, architecture, schema, impl-evidence, coverage-matrix, growth, collect, …) and judge against its excellence bar. Loading a producer's skill never licenses writing that producer's deliverable or spawning that hat. G-fresh is isolation, not a skill allowlist.
