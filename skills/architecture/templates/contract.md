# 技术方案 · <功能名>

> 上游：spec `<路径>`（FR-01…FR-0n，旅程 J-1…）· 选定设计方向 `02-shape/design-directions.md` · 产品 `architecture.md` / `domain-model.md`
> 泳道：L<n>｜appetite：<n>｜作者：architect 帽
> 下游：dba（数据语义）· 实现角色（票 + 契约）· qa（风险点）· sre（拓扑与容量）

## 0. 质量属性场景（本功能涉及的，驱动下面所有决策）

> 引用产品 `architecture.md` 已有 QAS-n；本功能新增的场景在这里定义，并回写产品层。

| id | 属性 | 场景（刺激 · 环境） | 响应 | 度量（目标） | 来源（NFR / 容量模型） |
|---|---|---|---|---|---|
| QAS-1 | 性能 | 打开报表，5 万行，高峰 | 首屏渲染 | P95 < 2s | NFR-01 |

容量估算（涉及新流量或新数据时必填）：

```
DAU <n> × 人均 <n> 次 / 86400 = 平均 QPS <n>；× 峰值系数 <n> = 峰值 QPS <n>
日增记录 <n> × <KB>/条 × 365 = 年增 <GB>
```

## 0.1 候选方案（≥2，真实可行，不是稻草人）

| 维度 | 方案 A <名> | 方案 B <名> | 方案 C <名>（可选） |
|---|---|---|---|
| 一句话 | | | |
| QAS-1 满足度（附依据：spike / 实测 / 估算） | | | |
| QAS-2 … | | | |
| 实现代价（人天） | | | |
| 运维代价（新依赖 / 监控 / 故障模式） | | | |
| 可演进性（到下一阶段要改什么） | | | |
| 主要风险 | | | |

**选定：** <方案> —— <理由>；**否决：** <方案 X 因为 …>（不可逆决策另写 ADR）
**复审触发条件：** <什么可观测信号出现时重新评估>

## 1. 现状测绘（改造类必填，新建写 N/A）

**现有边界**：<模块划分与依赖方向>

**约束**：

| 约束 | 来源 | 影响 |
|---|---|---|
| <不能动对外 API v1> | <有外部客户端在用> | <只能加字段不能改> |
| <既有 ADR-003 定了同步架构> | ADR-003 | <本次沿用或需 superseded> |

**本次不动什么**（防范围蔓延，比列出要动什么更重要）：

- <模块 X 的内部实现——只通过其现有接口调用>
- <表 Y 的结构——只读不改>

**已知技术债**（碰到就要小心，不在本轮修）：

| 位置 | 债 | 本次是否触碰 |
|---|---|---|

## 2. 模块边界

### 2.1 能力聚类

| FR | 能力拆解 | 归属模块 |
|---|---|---|
| FR-01 | <工单查询 · 数据转 CSV · 文件产出> | 工单域 + 导出能力 |
| FR-02 | <用户信息读取> | 用户域 |

**FR 归属核对**：每条 FR 都有归属，无遗漏、无重复归属。

### 2.2 模块清单

| 模块 | 一句话职责（不含「和」） | 变更理由 | 新建/既有 |
|---|---|---|---|
| <导出能力> | <把结构化数据按规则转成用户可下载的文件> | <支持新格式、改脱敏规则> | 新建 |

### 2.3 依赖图

```
<画出有向无环图，标明谁是编排者>

        工单域（编排者）
        ├──> 用户域（角色/组）
        ├──> 导出能力（CSV/脱敏）
        └──> 平台（审计/埋点/异步）
                  ▲
             通知能力 ┘
```

**无环确认**：☐ 已检查，无循环依赖
（有环时说明用了哪种解法：提取共用 / 依赖倒置 / 事件解耦）

### 2.4 耦合检查

| 检查项 | 结果 |
|---|---|
| 有无两模块共享表且都写 | 无 / 有（→ 所有者是 <X>，另一方走接口） |
| 有无模块含其它模块的知识（`if (来源 == X)`） | 无 |
| 边界强制手段 | 目录约定 / **静态检查脚本**（`<脚本路径>`，挂 lint 闸门） |

## 3. C4 模型

### Context

```
<系统与外部角色/外部系统的关系>
```

<一段文字说明：为什么边界画在这里，哪些是我们的责任>

### Container

```
<可独立部署的单元：应用/服务/数据库/队列/存储>
```

<一段文字说明：为什么这样切分部署单元>

### Component（涉及多模块时）

```
<Container 内的模块划分 = 第 2 节的产出>
```

**不画类图。** 类的设计归实现角色。

## 4. 可行性验证（spike）

| spike | 问题（可判真假） | 时限 | 结论 |
|---|---|---|---|
| <CSV 生成性能> | <单机能否 10 秒内生成 10 万行> | <半天> | <见下> |

```
spike：<名称>
问题：<一句话>
环境：<硬件 / 运行时 / 数据规模>
结果：<带数字>
结论：<决策影响：所以我们决定 ...>
```

**无 spike 时显式声明**：本方案未使用未验证技术，无外部依赖不确定性，性能要求距已知能力上限有 <n> 倍余量。

## 5. 关键决策

| 决策 | 结论 | ADR |
|---|---|---|
| <同步还是异步> | <10000 行内同步，超出异步> | [ADR-005](adr-005-xxx.md) |
| <存储选型> | <沿用 MySQL，不引入新存储> | 无需 ADR（沿用既有，非新决策） |

不可逆决策必须有 ADR。判据：三个月后想改，成本是「改几行」还是「改一片」。

## 6. 契约

| 契约 | 路径 | 消费方 |
|---|---|---|
| API | [contracts/export-api.md](contracts/export-api.md) | `/backend` `/frontend` `/qa` |
| 事件 | [contracts/export-events.md](contracts/export-events.md) | 审计服务、埋点服务 |
| 数据语义（给 /dba） | 见下 7 节 | `/dba` |

## 7. 数据语义诉求（给 /dba，不写表结构）

```
实体诉求：
  <导出任务> —— 一行 = <一次导出请求>
    需要记录：<谁发起、筛选条件、行数、状态、文件位置、失败原因>
    生命周期：<创建 → 处理中 → 完成/失败；文件保留 7 天>
    访问模式：<按发起人查最近 10 条；按状态查待处理>

约束诉求：
  <同一用户同一分钟内只能有一个进行中的任务>
  → 唯一约束还是应用层判断？请 /dba 判断
```

## 8. 角色裁剪声明

| 角色 | 参与 | 理由 |
|---|---|---|
| `/dba` | 是 | 新增导出任务表 |
| `/designer` | 是 | 导出入口与进度提示需要设计 |
| `/algo` | **N/A** | 纯规则逻辑，无 AI 需求 |
| `/miner` | **N/A** | 无离线建模需求 |
| `/analyst` | 是 | 度量蓝图复盘 |

用不上的显式声明 N/A。默默跳过无法区分「不需要」和「忘了」。

## 9. Rabbit Holes（会掉进去的坑，提前标出）

| 坑 | 为什么危险 | 边界 |
|---|---|---|
| <大数据量导出的内存管理> | <一次性加载会 OOM，spike 已验证 50 万行占 1.6GB> | <本轮上限 10000 行；流式写入下轮> |
| <文件格式扩展性> | <容易过度设计成插件框架> | <本轮只做 CSV，硬编码；第二种格式出现时再抽象> |

**「第二次出现时再抽象」是防过度设计的默认纪律。**

## 10. 任务拆解（按用户旅程纵向切片）

> 一张票 = 一段可以端到端走通的旅程切片（后端 + 前端 + 测试 + 联调）。不要按「表 / 接口 / 页面」横切——横切的票全部完成了，旅程仍可能走不通。

| 票 | 旅程切片 | FR 锚点 | 角色（后端 → 前端） | 契约 / mock | 联调验收（在运行的产品上看到什么） | 依赖 | 粗估 |
|---|---|---|---|---|---|---|---|
| T-01 | J-1 第 1–2 步：导入数据 | FR-01 FR-02 | backend → frontend | `contracts/import-api.md` + 示例响应 | 上传 5000 行样例，列表出现数据集，截图 | — | 2–3d |
| T-02 | J-1 第 3 步：生成并看到报表（Aha） | FR-03 | backend → frontend | `contracts/report-api.md` | 30 秒内看到报表首屏，签名时刻动效存在 | T-01 | 3–4d |

**并行度检查**：同一切片内后端先发契约示例，前端可并行开发；切片之间依赖尽量只在数据上。
**纵向检查**：每张票结束时都能在运行中的产品上演示一段旅程——做不到说明切成了横向。

**总粗估**：<n>–<n> 人天。给 `/pm` 作 RICE 的 Effort 输入。

## 11. 风险点（给 /qa 与 /sre）

| 风险 | 影响 | 给谁 | 建议关注 |
|---|---|---|---|
| <异步任务失败后用户无感知> | <用户以为在跑，实际已死> | `/qa` | <必测：worker 崩溃后的状态> |
| <导出查询可能拖慢列表> | <护栏指标 NFR-01> | `/sre` | <监控列表 P95> |

## 12. Security findings `[SEC-n]`（Q-security）

Q-security = Q2 auth/tenant **or** Q3 external contract **or** Q4 irreversible. If none: write `N/A（Q-security=no）`. Walk: `architecture/references/threat-model.md`.

| ID | Boundary | STRIDE | Mitigation | Downstream |
|---|---|---|---|---|
| [SEC-1] | <trust boundary> | <S/T/R/I/D/E> | <one line in this contract or NFR> | NFR-? / qa case / residual |

Unmitigated residual stays on §11 风险点. Silent acceptance is a defect. No separate `threat-model.md` file.

## 13. 变更记录

| 版本 | 日期 | 变更 | 触发者 |
|---|---|---|---|
| v1 | <日期> | 初版 | — |
