<!-- sdlc:unfilled — architect 写入本产品的真实内容后删除本行 -->
# 架构基线 · <产品名>

> 产品层文件（负责人：architect）。系统现在是什么样、要满足哪些质量目标、怎么演进。每个功能的 `contract.md` 都是对这里的增量。
> 读者：dba · backend · frontend · sre · qa · growth（容量与上线节奏）

## 1. 系统上下文

```
<用户 / 外部系统 / 我们的系统之间的关系；标出信任边界>
```

## 2. 容器视图

| 容器（可独立部署） | 职责 | 技术 | 数据存储 | 伸缩方式 |
|---|---|---|---|---|

## 3. 质量属性场景（驱动所有架构决策）

> 每条场景可测：刺激 → 环境 → 响应 → 度量。没有度量的「高性能、高可用」不算场景。

| id | 属性 | 场景（来源 · 刺激 · 环境） | 响应 | 度量（目标） | 当前实测 | 状态 |
|---|---|---|---|---|---|---|
| QAS-1 | 性能 | 用户打开报表（J-1 第 3 步），5 万行数据，高峰期 | 首屏图表渲染 | P95 < 2s | <值 / 未测> | 达标 / 风险 |
| QAS-2 | 可伸缩 | 活动带来 10 倍 DAU | 系统不降级 | 峰值 QPS <n> 下错误率 < 0.1% | | |
| QAS-3 | 可用性 | 单可用区故障 | 自动切换 | RTO < 5min，RPO < 1min | | |
| QAS-4 | 可演进 | 新增一种数据源 | 只加适配器 | 改动 ≤ 1 个模块，无迁移 | | |
| QAS-5 | 安全 | 越权访问他人租户数据 | 拒绝并审计 | 0 次泄露；审计 100% | | |
| QAS-6 | 成本 | 每千活跃用户月成本 | | < <金额> | | |

## 4. 容量模型

| 输入（来自 strategy / growth / 分析） | 值 | 来源 |
|---|---|---|
| DAU（当前 / 12 个月后目标） | | |
| 人均关键请求数 / 天 | | |
| 峰值系数（峰值 QPS / 平均 QPS） | | |
| 单条记录体积 · 日增记录数 | | |

推导：

```
平均 QPS = DAU × 人均请求 / 86400
峰值 QPS = 平均 QPS × 峰值系数
存储年增量 = 日增记录数 × 单条体积 × 365 × (1 + 索引与副本系数)
```

| 资源 | 当前 | 12 个月后 | 瓶颈点 | 应对（触发条件） |
|---|---|---|---|---|

## 5. 技术选型与雷达

| 技术 | 用途 | 状态（采用 / 试验 / 评估 / 暂缓） | ADR |
|---|---|---|---|

## 6. 架构约束与适应度函数

| 约束 | 为什么 | 自动检查（命令 / CI job） |
|---|---|---|
| <模块边界：router 不引用 ORM> | 分层 | `<检查命令>` |
| <核心接口 P95> | QAS-1 | `<性能冒烟命令>` |

## 7. 演进路线

| 阶段 | 触发条件（可观测） | 架构变化 | 代价 |
|---|---|---|---|
| 现在 | — | <单体 + 单库 + Redis> | — |
| 阶段 2 | <DAU > 10 万 或 报表查询 P95 > 2s> | <分析查询迁至列式库 / 读写分离> | |
| 阶段 3 | <…> | | |

## 8. ADR 索引

| ADR | 决策 | 状态 | 日期 |
|---|---|---|---|

## 9. 已知技术债

| 位置 | 债 | 影响的 QAS | 计划 |
|---|---|---|---|
