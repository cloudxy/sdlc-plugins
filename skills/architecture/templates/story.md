<!-- sdlc:unfilled — 写入真实内容后删除本行；未填写的模板不算交付 -->
# T-<n> · <可演示的消费者结果>

> 状态：todo / doing / blocked / done｜父切片：<本票或所属切片>｜slice_integrator：<一个角色>
> FR / J / NFR / SEC：<canonical 路径@版本 + ID；基础设施任务引用其质量目标，不编造 UI 旅程>
> 完成结果：<消费者通过 UI/API/SDK/任务输出能验证什么>

## 输入与范围

| 输入 | 唯一路径 / 版本 / 使用片段 |
|---|---|
| 需求 | <spec@revision + FR/GWT/J ID；不复制可变验收正文> |
| 契约 | <accepted contract/API/event@revision + 示例/mock> |
| UI（如适用） | <prototypes/final 源码入口@revision、组件 ID、tokens、flows、edge-states；截图辅助> |
| 数据/埋点（如适用） | <DBA schema/db-spec 和数据字典/事件规范的版本> |

本票范围：<业务结果、文件写权限、排除项>。子任务排除的工作必须指出所属父切片与负责子任务，不能把 API 票单独标成已完成的 UI 旅程。

## 专业子任务与依赖

| 子任务 | 负责人 | 输入依赖 | 输出 / 写入范围 | 状态 |
|---|---|---|---|---|

契约与样例就绪后可并行；共享写入和前置数据依赖需排序。一个父切片由 integrator 负责联合关闭。

## 验收映射

| GWT / NFR / SEC 的来源 ID | 验证方式 / 环境 | 证据记录链接 | 状态 |
|---|---|---|---|

不重写上游标准；按 ID 引用。每条义务有真实证据，不能只勾选。

## 集成完成条件

- 环境、启动/执行命令与真实依赖：<路径/命令>。
- UI：运行中的实现逐状态对照定稿原型源码、组件 ID、tokens、flows；保留截图。
- 非 UI：通过 API/SDK 示例、批任务输出、流处理结果或设备运行验证消费者结果；不要求虚构页面截图。
- 集成记录：`03-impl/T-<n>-integration.md`；实现证据引用各角色的 canonical evidence 文件，记录实际命令、退出码、环境、版本和偏差。

## 变更与阻塞

| 发现 / 证据 | 影响的输入版本 | 负责角色 | 待决策 / 返工 | 复验 |
|---|---|---|---|---|

实现中标准不可达，交 architecture/change-impact 与相关业务负责人；不自行降低标准。所有专业子任务、适用检查与真实集成通过后，integrator 才能关闭父切片。
