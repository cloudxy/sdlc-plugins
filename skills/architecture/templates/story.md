<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# T-<nn> · <标题>

> 泳道：L<n>｜角色：backend → frontend（同一切片）｜粗估：<n>–<n>d｜状态：**todo** / doing / done / blocked
> **旅程切片**：J-<n> 第 <a>–<b> 步 ——完成时在运行的产品上能演示：<看到什么>
> 上游工件（必读，不要凭票面猜）：
> - 为什么做：`00-discover/briefing.md`（核心价值 / 亮点）· spec §2 核心价值与 Aha
> - PRD：`<路径>` FR-<n> · 旅程 J-<n>
> - 设计：`02-shape/flows.md` · `02-shape/edge-states.md` · 选定原型截图 `02-shape/prototypes/<d>-*.png`
> - 方案：`<路径>` 第 <n> 节 · 契约：`<路径>`（后端先交示例响应 / mock）
> - 数据：`<db-spec 路径>` / `<schema.dbml 路径>` · 埋点：`01-define/tracking.md` EV-<n>

## FR 锚点

> 无锚点的票无法验收。每票必须指明它实现哪条 FR 的哪几条 GWT。

| FR | GWT | 本票是否完整覆盖 |
|---|---|---|
| FR-01 | GWT-01.1, 01.2, 01.4 | 是 |
| FR-07 | GWT-07.1 | 部分（脱敏规则由 T-05 提供） |

## 边界

**本票做什么**：<一两句>

**本票不做什么**（防蔓延）：

- <脱敏规则的实现——由 T-05 提供，本票只调用>
- <前端展示——由 T-04 负责>

**会改哪些文件**（计划阶段填，实现时对照）：

| 文件 | 改动性质 |
|---|---|
| `<path>` | 新增 |
| `<path>` | 修改 |

**不许改的文件**：<对外契约 / 他票正在改的文件>

## 依赖

| 前置票 | 需要它提供什么 | 状态 |
|---|---|---|
| T-01 | 导出任务表结构 | done |

**并行安全性**：本票与 <T-0x> 并行时不改同一文件 ☐ 已确认

## 验收清单

> 直接引用 GWT，不要重新表述（重新表述会引入偏差）。

- [ ] GWT-01.1：Given 主管已登录且本周有 30 条未结工单 / When 全选并导出 / Then 得到含 30 行的 CSV
- [ ] GWT-01.2：空态——提示「无可导出数据」，不产生文件
- [ ] GWT-01.4：超限——10001 条时提示并拒绝
- [ ] GWT-07.1：手机号中间四位掩码

**NFR 相关**：

- [ ] NFR-01：P95 < 800ms（附实测）
- [ ] NFR-04：导出行为记审计日志

## 联调验收（切片完成的定义）

| 项 | 内容 |
|---|---|
| 启动 | `sdlc.config.yaml` 的 `app.start` |
| 走查路径 | J-<n> 第 <a>–<b> 步（真实后端，不用 mock） |
| 截图 | `bash PLUGIN_ROOT/scripts/ui-evidence.sh <url> 03-impl/screens/T-<nn> 375,1440` |
| 对照 | 选定原型截图；签名时刻是否出现 |
| 记录 | `03-impl/T-<nn>-integration.md`：命令、截图路径、与契约/设计的偏差 |

## 自测命令

> 交票时命令与退出码**原样粘贴**到下方 evidence 区。「基本完成」「应该没问题」不接受。

```bash
<测试命令>          # 如 uv run pytest -q backend/tests/test_export.py
<架构检查>          # 如 bash scripts/check-arch.sh
<构建>              # 如 npm run build
```

## 进度记录

> 实现过程中在这里记录，不要只在会话里说。

| 时间 | 动作 | 说明 |
|---|---|---|
| | | |

## Evidence（交票前必填）

```
$ <命令>
<输出>
exit: 0
```

**每条验收项都要有对应证据。** 无证据的勾选视为未完成。

## 阻塞记录（有则填）

| 阻塞原因 | 需要谁 | 上报时间 |
|---|---|---|
| <验收标准 GWT-01.4 与契约的 422 语义冲突> | `/architect` | <时间> |

**实现中发现验收标准不可达 → 停下来上报，不要自行改标准。** 改标准是 `/pm` 的权限，改契约是 `/architect` 的权限。自行降级实现是最常见的质量逃逸路径。

## 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿（非「大部分通过」）
- [ ] 只改了「会改哪些文件」里列出的文件
- [ ] 未触碰「不许改的文件」
- [ ] 发现的上游问题已回报，不是自行绕过
- [ ] 状态已更新为 done
