# ADR-<nnn>：<决策一句话>

> 状态：proposed / **accepted** / superseded by ADR-<nnn> / deprecated
> 日期：<YYYY-MM-DD>｜决策者：architect 帽｜相关：spec FR-<n>、`02-shape/contract.md`

## 背景

<两三句：什么情况下需要做这个决定。要让三个月后的人不读别的文档就能理解为什么这个决定是必要的。>

**触发这个决策的约束**：

| 约束 | 来源 |
|---|---|
| <P95 必须 < 100ms> | NFR-01 |
| <多实例部署，需缓存一致> | 现有部署拓扑 |
| <团队无 X 技术经验> | 团队现状 |

## 决策

<一句话，明确到可执行。存储类决策必须写清「哪个事实在哪」，禁止用一个品牌回答复合名词（如「调度状态」= 协调 + 记录）。>

<必要时补充关键细节，但不要写成实现文档——实现细节归 contract.md 和代码。>

## 备选与否决理由

> **这一节是 ADR 的全部价值。** 只写「我们决定用 X」的 ADR 等于没写。

### 备选 A：<方案名>

<一句话描述>

**否决理由**：<必须带证据或具体约束，不能是「不够好」>

例：多实例部署下缓存不一致，本场景要求 30 秒内全实例一致（NFR-03）

### 备选 B：<方案名>

**否决理由**：<...>

例：已做 spike（见下方证据），P95 仍为 340ms，NFR-01 要求 < 100ms

### 备选 C：<方案名>

**否决理由**：<...>

例：需要 Sorted Set 做排行榜（FR-08），该方案无此数据结构

## 证据

<spike 结论、实测数字、外部资料。带数字的结论才能支撑决策。>

```
spike：<名称>
问题：<一句话，可判真假>
环境：<硬件 / 运行时 / 数据规模>
结果：<数字>
结论：<对决策的影响>
```

无 spike 时写明依据来源（官方文档某节、既有项目经验、社区共识 + 链接）。

## 代价与风险

**我们接受的代价**（诚实列出，不要只写好处）：

| 代价 | 缓解措施 |
|---|---|
| <新增运维依赖> | <已在 release-checklist 加监控项> |
| <需处理缓存穿透/雪崩> | <见 02-shape/contract.md 缓存策略段> |
| <最终一致，延迟窗口 30 秒> | <期间用户看到旧值；已在 PRD 确认可接受> |

**选最终一致时必须回答三问**：可接受的延迟窗口多长？期间用户看到什么？不一致被发现时怎么修（有无对账）？

## 影响范围

| 谁 | 需要做什么 |
|---|---|
| `/backend` | <按此决策实现，注意 X> |
| `dba` | <数据语义诉求见 02-shape/contract.md 第 7 节> |
| `/sre` | <新增依赖的监控与告警> |
| `/qa` | <必测：缓存失效路径、依赖不可用时的降级> |

## 后续复审条件

<什么情况下应该重新审视这个决策。写出来，避免它成为永远不被质疑的教条。>

例：数据量超过 1000 万行时重新评估存储选型；或团队引入了 X 能力后重新评估自研 vs 引入。

---

## 变更历史

| 日期 | 状态变化 | 说明 |
|---|---|---|
| <日期> | proposed → accepted | 评审通过 |

**永不删除 ADR，永不原地改已 accepted 的决策。** 改判就新写一份，把旧的标 `superseded by ADR-nnn` 并保留原文——决策的演进史记录了团队认知的变化，本身就是重要信息。
