<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# 测试报告 · <功能名>

> 版本/commit：`<sha>`｜环境：<测试/预发>｜作者：/qa｜日期：<YYYY-MM-DD>
> 覆盖矩阵：`<04-verify/coverage.md 路径>`

## 1. 结论先行

| 项 | 结论 |
|---|---|
| 本轮测试结论 | <通过 / 有条件通过（列条件） / 不通过（列阻塞项）> |
| blocker 缺陷 | <n> 个未修复 |
| major 缺陷 | <n> 个未修复 |
| 覆盖空洞 | <n> 个（详见矩阵第 5 节） |
| 未在真实环境验证 | <n> 项 |

**放行决策不由我做**（铁律：产出者不批准自己的工件）→ `/qc` 或 `/reviewer`。本报告只陈述事实。

## 2. 分层执行结果

> 命令与输出**原样粘贴**，退出码必须有。

### 单元测试

```
$ <命令>
<原样输出>
exit: 0
```

| 项 | 数字 |
|---|---|
| 通过 / 失败 / 跳过 | <n> / <n> / <n> |
| 耗时 | <n>s |

### 集成测试

```
$ <命令>
<原样输出>
exit: 0
```

| 项 | 数字 |
|---|---|
| 通过 / 失败 / 跳过 | <n> / <n> / <n> |
| 数据库类型 | <MySQL 8 via Docker / SQLite（**保真度风险，见第 5 节**）> |

### E2E

```
$ <命令>
<原样输出>
exit: 0
```

| 项 | 数字 |
|---|---|
| 通过 / 失败 | <n> / <n> |
| 覆盖的主干路径 | <列出，如「登录→筛选→导出→下载」> |

### 专项（性能/容量，有则填）

```
$ <命令>
<原样输出>
```

| NFR | 要求 | 实测 | 环境 | 结论 |
|---|---|---|---|---|
| NFR-01 | P95 < 800ms | <数字> | <数据量/硬件> | ✅ / ❌ |

**跳过的用例必须解释**——`skipped` 数量不为 0 时逐条说明为什么跳过。

## 3. 缺陷清单

| 编号 | 标题 | 严重度 | 违反的 GWT | 状态 | 指派 |
|---|---|---|---|---|---|
| BUG-011 | 10000 条边界返回 422（应通过） | **blocker** | GWT-01.3 | 已修复待验 | `/backend` |
| BUG-012 | 无法构造部分失败场景（可测性） | major | GWT-01.6 | open | `/backend` |
| BUG-013 | 空态文案未区分筛选后无结果 | minor | GWT-01.2 | open | `/frontend` |

### 严重度分布

| 级别 | 总数 | 已修复 | 待验证 | open |
|---|---|---|---|---|
| blocker | <n> | <n> | <n> | <n> |
| major | <n> | <n> | <n> | <n> |
| minor | <n> | <n> | <n> | <n> |

**全是 blocker 等于没有分级。** 严重度判据见 SKILL.md。

## 4. 回归执行

| 项 | 内容 |
|---|---|
| 影响面推导 | <直接影响：导出模块｜调用方：工单列表页｜共享数据：tickets 表 status 字段｜共享配置：SYNC_THRESHOLD｜已知坑：BUG-007 相关用例> |
| 回归集规模 | <n> 条 |
| 结果 | <n> 通过 / <n> 失败 |

```
$ <回归命令>
<原样输出>
exit: 0
```

**本轮新增回归用例**（每个修复的缺陷都要有）：

| 用例 | 对应缺陷 | 说明 |
|---|---|---|
| TC-R011 | BUG-011 | 10000 条界上必须通过 |

## 5. 保真度与未验证项

> 这一节让 `/qc` 知道哪些行为**未经真实验证**。

### 环境差异风险

| 项 | 测试环境 | 生产环境 | 风险 |
|---|---|---|---|
| 数据库 | <SQLite> | <MySQL 8> | <类型约束、并发行锁、方言函数未验证> |
| 数据量 | <1000 行> | <百万行> | <N+1、全表扫、性能未验证> |
| 实例数 | <单实例> | <多实例> | <缓存一致性、分布式锁未验证> |

### 需真库验证清单（转 /sre）

| 用例 | 原因 | 状态 | 结果 |
|---|---|---|---|
| TC-030 | 并发行锁行为 | 待执行 / 已执行 | <输出> |

### mock 掉的依赖

| 依赖 | mock 方式 | 未验证的真实行为 |
|---|---|---|
| <对象存储> | <内存 stub> | <真实超时、限流、错误格式> |

**标注了但没人跑等于没标注。** 状态要跟踪到闭环。

## 6. 覆盖空洞摘要

> 明细在 `04-verify/coverage.md` 第 5 节，这里只列结论供快速判断。

| 空洞 | 类型 | 风险 | 是否阻塞发布 |
|---|---|---|---|
| GWT-01.6 部分失败 | ❌ 缺口 | 该分支未验证，行数可能不符且无提示 | <是/否 + 理由> |
| NFR-01 性能 | ⚠️ 需真实环境 | 性能要求未验证 | <是/否> |

## 7. 给下游的信息

| 给谁 | 内容 |
|---|---|
| 实现角色 | 缺陷清单（第 3 节），逐条含复现步骤 |
| `/qc` / `/reviewer` | 本报告 + 覆盖矩阵；重点看第 5、6 节 |
| `/pm` | <GWT 不可测的反馈：哪几条需要补判据> |
| `/sre` | 需真库验证清单（第 5 节） |
| `/architect` | <契约歧义：哪些行为契约没定，测试时被迫假设> |

## 8. 自检

- [ ] 结论先行，且未替 `/qc` 做放行决策
- [ ] 每层执行命令与退出码原样贴
- [ ] `skipped` 用例逐条解释
- [ ] 缺陷有严重度分级（不是全 blocker）
- [ ] 每个缺陷指明违反的 GWT 编号
- [ ] 回归影响面五个维度都推导过
- [ ] 每个修复的缺陷有新增回归用例
- [ ] 环境差异风险已列
- [ ] mock 掉的依赖已列
- [ ] 需真库验证清单已转 `/sre` 且状态在跟踪
- [ ] 覆盖空洞已摘要，标明是否阻塞
