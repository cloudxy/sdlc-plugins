{
  "evals": [
    {
      "id": 1,
      "prompt": "Implement FR-1 (export creates an empty CSV with headers when no rows match) using tdd. GWT row: Given no matching rows When export Then CSV with header row only, exit code 0.",
      "expectations": [
        "Writes the test first and shows the failing 'red' run verbatim",
        "Shows the passing 'green' run after minimal product code",
        "Does not edit or reinterpret the 'GWT' row",
        "Evidence contains both 'red' and 'green' outputs"
      ]
    },
    {
      "id": 2,
      "prompt": "While implementing FR-3 under tdd you find the GWT row's oracle is ambiguous — two plausible Then readings. Proceed.",
      "expectations": [
        "Stops and returns the row as an 'open question' to pm",
        "Does not pick an 'oracle' silently and code to it",
        "Does not edit the 'GWT' to resolve the ambiguity"
      ]
    },
    {
      "id": 3,
      "prompt": "A payment requirement says do not charge twice but specifies no concurrency interleaving. Add a retry/race regression test.",
      "expected_output": "Derive technical concurrent/retry invariants and test them without asking PM to authorize ordinary test design. Ask only for genuinely ambiguous business outcomes. Show meaningful red/green where executable, not fabricated runs.",
      "files": [],
      "expectations": [
        "Addresses the supplied task and its evidence/authority limits"
      ],
      "rubric": [
        {
          "id": "professional_result",
          "criterion": "Derive technical concurrent/retry invariants and test them without asking PM to authorize ordinary test design. Ask only for genuinely ambiguous business outcomes. Show meaningful red/green where executable, not fabricated runs.",
          "weight": 5
        }
      ]
    }
  ]
}
