<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# 发布通告 · v<版本> · <日期>

> 作者：/ops｜面向：**用户**（不是开发）
> 写用户能感知的变化，不写内部实现。

---

## 本次更新

### 🎉 新功能

**<批量导出工单>**

<一两句说明用户现在能做什么，以及它解决了什么麻烦。>

例：现在可以一次导出多条工单为 CSV 文件。此前需要逐条复制，30 条约需 20 分钟；现在筛选后全选，几秒完成。

| 项 | 说明 |
|---|---|
| 影响谁 | <使用工单列表的主管与客服> |
| 怎么用 | <工单列表 → 筛选 → 全选 → 点击「导出」> |
| 限制 | <单次最多 10000 条；超过会转为后台导出并在完成后通知你> |

### ⚡ 改进

- **<导出速度>**：<1000 条工单现在 3 秒内完成，此前约 15 秒>
- **<列表加载>**：<首屏显示加快约 30%>

**说可感知的数字**，不说「优化了性能」。

### 🐛 修复

> 逐条列出，**不写「修复了若干问题」** —— 用户不知道自己遇到的问题修了没有，还会再提一次工单。

- 修复：<导出文件在 Excel 中打开时中文乱码>
- 修复：<筛选条件在刷新页面后丢失>
- 修复：<工单标题过长时列表布局错位>

### ⚠️ 破坏性变更

> 有则**显著标注**，并给迁移路径与时间。无则删除本节。

**<导出接口字段调整>**

| 项 | 内容 |
|---|---|
| 变更内容 | <响应字段 `user_name` 更名为 `username`> |
| 影响谁 | <通过 API 集成导出功能的客户> |
| **迁移路径** | <两个字段将同时返回至 <2026-12-31>；之后仅保留 `username`> |
| **下线时间** | <2026-12-31> |
| 需要你做什么 | <在此日期前将代码中的 `user_name` 改为 `username`> |
| 有问题联系 | <支持渠道> |

**破坏性变更必须给足过渡期**，且过渡期内新旧并存（见 `/architect` 的 contract-design 版本策略）。

---

## 已知问题

> 诚实列出，避免用户重复报同一问题。

| 问题 | 影响 | 计划 |
|---|---|---|
| <导出的列暂不支持自定义顺序> | <需要特定顺序的用户要手工调整> | <下个版本> |
| <异步导出完成后仅站内通知，无邮件> | <离开系统的用户不能及时得知> | <评估中> |

---

## 写作检查

- [ ] 全文无内部实现术语（Service、重构、接口、SQL、缓存…）
- [ ] 每项说清「影响谁」
- [ ] 改进项带可感知的数字（不是「优化了性能」）
- [ ] 修复项逐条列出，**无「若干问题」**
- [ ] 破坏性变更显著标注 + 迁移路径 + 下线时间 + 需要用户做什么
- [ ] 已知问题诚实列出
- [ ] 限制条件写清（如单次上限）
- [ ] 未承诺未确定的上线时间（用「评估中」而非具体日期）

## 常见错误对照

| ❌ 写法 | ✅ 改写 |
|---|---|
| 优化了 ExportService 的查询性能，重构了分页逻辑 | 导出速度提升，1000 条工单现在 3 秒内完成 |
| 修复了若干已知问题 | 修复：导出文件中文乱码；修复：筛选刷新后丢失 |
| 提升了系统稳定性 | 修复：高峰期偶发的导出失败 |
| 新增批量导出功能 | 现在可以一次导出多条工单，此前需逐条复制 |
| 调整了 API 字段 | ⚠️ `user_name` 更名为 `username`，旧字段将于 12-31 下线 |
| 下个版本将支持自定义列 | 自定义列顺序：计划中（未定具体版本） |

**最后一条**：不要承诺未确定的时间。承诺了就要兑现，兑现不了的承诺比不承诺更伤信任。
