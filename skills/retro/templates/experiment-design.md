# 实验设计 · <实验名>

> 作者：/analyst｜日期：<YYYY-MM-DD>｜状态：设计中 / 运行中 / 已结束
> **预先注册**：本文件在实验开始前冻结。事后任何改动都必须标注为探索性分析。

## 0. 支撑什么决策

| 项 | 内容 |
|---|---|
| 要决定什么 | <是否把新导出入口全量上线> |
| **决策规则（事前写死）** | <使用率提升 ≥ 3 个百分点且护栏未破 → 全量；< 1 个百分点 → 回滚；1–3 之间 → 延长观察> |

**决策规则必须事前定** —— 否则数字出来后会被解释成想要的结论。

## 1. 假设（可证伪）

<把导出入口从二级菜单移到列表工具栏，会使导出功能周使用率从 12% 提升至 15% 以上。>

| 检查 | |
|---|---|
| 含具体改动 | ☐ |
| 含具体指标 | ☐ |
| 含基线值 | ☐ |
| 含预期方向与幅度 | ☐ |

❌ 反例：「新入口会提升用户体验」（无法证伪）

## 2. OEC 与护栏

### OEC（只能一个）

| 项 | 内容 |
|---|---|
| 指标 | <导出功能周使用率> |
| 口径 | <分子：触发 export_clicked 的主管去重数；分母：访问过列表的主管去重数> |
| 基线 | <12.0%（过去 4 周均值）> |
| 目标 | <≥ 15%> |

**两个 OEC 等于没有 OEC** —— 冲突时无法决策。

### 护栏（必须有，带红线值）

| 指标 | 红线 | 为什么需要 |
|---|---|---|
| <列表页 P95> | <≤ 1000ms> | <新入口增加渲染负担> |
| <导出失败率> | <不高于基线 + 1%> | <使用量上升可能压垮后端> |
| <整体工单处理时长> | <不上升> | <防止用户被新入口分散注意力> |

## 3. 分流

| 项 | 内容 |
|---|---|
| **分流单位** | <用户 / **租户**>（有交互效应时必须用租户） |
| 交互效应判断 | <同租户内主管会互相讨论界面差异 → **按租户分流**> |
| 随机化方式 | <hash(tenant_id + "exp_export_entry_v1") % 100> |
| 比例 | <50 / 50> |
| 排除人群 | <内部租户、试用租户（行为不代表真实用户）> |

**分流单位选错会污染实验**：多租户系统按用户分流时，同租户内两组用户互相影响，效应被稀释。

### 分组均衡性检验（事前必做）

| 组 | 样本量 | 历史 OEC | 历史工单量/周 | 判定 |
|---|---|---|---|---|
| A（对照） | <n> | <11.8%> | <42> | |
| B（实验） | <n> | <12.1%> | <41> | ☐ 差异在正常波动内 |

**不检验就可能一开始就有偏差**，结果无法解释。若发现显著差异 → 换 salt 重新分流。

## 4. MDE 与样本量

| 项 | 值 | 依据 |
|---|---|---|
| 基线 | <12%> | 过去 4 周 |
| **MDE** | <+3 个百分点> | **从业务价值倒推**：改动成本 2 人周，每提升 1 个点约值 ￥<n>/月 → 需 3 个点回本 |
| 显著性 α | 0.05（双侧） | |
| 功效 1-β | 0.8 | |
| **每组所需样本** | <n> | <计算工具/公式> |
| 日均可分配流量 | <n> | |
| **预计所需时长** | <n> 天 | |

### 样本量不足时的选择（若适用）

| 选择 | 是否采用 |
|---|---|
| 放大 MDE（只检测更大效应） | ☐ |
| 延长实验 | ☐ |
| 换更高流量的入口 | ☐ |
| **放弃实验，改用灰度观察 + 定性研究** | ☐ |

**不要在样本量不足时硬跑** —— 不显著时无法区分「没效果」与「样本不够」。

## 5. 停止规则（禁止 peeking）

| 项 | 内容 |
|---|---|
| **何时看 OEC** | <跑满 14 天或达到每组 <n> 样本，取先到者> |
| 中途可看什么 | **仅护栏指标**（为及时止损） |
| 中途不可看什么 | **OEC** |
| 方法 | <固定时长 / 序贯检验（若用，说明实现）> |

**反复看结果并在显著时停止 = 假阳性率飙升。** 这是最常见的作弊方式，很多人不知道自己在做。

### 时长的其它考虑

- [ ] 覆盖至少 1 个完整周（周末与工作日行为不同），最好 2 周
- [ ] 考虑新奇效应（新界面初期短暂提升，2 周后消退）
- [ ] 考虑学习效应（复杂功能需时间学会）
- [ ] 未跨大促/节假日

## 6. 回滚阈值（任一满足即停止，不讨论）

- ❌ <列表页 P95 > 1000ms（护栏红线）>
- ❌ <导出失败率 > 基线 + 2%>
- ❌ <出现数据不一致>
- ❌ <P0 告警>

与 `/sre` 的灰度回滚判据一致 —— **提前写死，不临场判断**。

## 7. 预先定义的切片（防 p 值猎取）

> 事后才想到的切片是探索性的，不能直接下结论。

| 切片 | 为什么预先关注 |
|---|---|
| <桌面 vs 移动> | <入口位置在两端布局不同> |
| <租户规模（大/中/小）> | <大租户主管数多，行为可能不同> |

**未列在此处的切片，事后分析只能作为探索性发现**，需要新实验验证。

## 8. 结果（实验结束后填）

### OEC

| 项 | A（对照） | B（实验） | 差异 |
|---|---|---|---|
| 样本量 | <n> | <n> | |
| OEC | <11.9%> | <15.4%> | **<+3.5 个百分点>** |
| 95% CI | | | <[1.8, 5.2]> |
| p 值 | | | <0.002> |

**置信区间比 p 值信息量更大**：<CI 下界 1.8 > 0，方向确定；但幅度可能小到 1.8（低于 MDE）也可能大到 5.2。>

### 护栏

| 指标 | A | B | 红线 | 判定 |
|---|---|---|---|---|
| 列表页 P95 | <810ms> | <835ms> | ≤1000ms | ✅ |
| 导出失败率 | <0.3%> | <0.4%> | ≤1.3% | ✅ |
| 处理时长 | <持平> | <持平> | 不上升 | ✅ |

### 预先定义的切片

| 切片 | A | B | 差异 | 备注 |
|---|---|---|---|---|
| 桌面 | <n> | <n> | <+4.1> | |
| 移动 | <n> | <n> | <+1.2> | <移动端提升较小> |

### 结论与决策

| 项 | 内容 |
|---|---|
| 是否达到事前决策规则 | <是：+3.5 ≥ 3 且护栏未破 → **全量上线**> |
| 不显著时的说明（若适用） | <三种可能：真无效 / 样本不足（实际功效 <x>）/ 子群相反> |
| 探索性发现（需新实验验证） | <移动端提升明显小于桌面端，值得单独优化> |

## 9. 自检

**设计阶段**

- [ ] 说清支撑什么决策；**决策规则事前写死**
- [ ] 假设可证伪（含改动+指标+基线+幅度）
- [ ] OEC 只有一个
- [ ] 护栏 ≥2 个且有红线值
- [ ] 分流单位考虑了交互效应
- [ ] 随机化保证同一单位始终同组
- [ ] **分组均衡性已事前检验**
- [ ] MDE 从业务价值倒推
- [ ] 样本量已估算；不足时已做选择（未硬跑）
- [ ] 停止规则已定，**禁止 peeking OEC**
- [ ] 时长覆盖完整周期，考虑新奇/学习效应
- [ ] 回滚阈值已定，与 `/sre` 一致
- [ ] **切片预先定义**（防 p 值猎取）
- [ ] 本文件在实验开始前冻结

**结果阶段**

- [ ] 未中途 peeking OEC
- [ ] 报告了效应大小 + 置信区间 + 样本量 + p 值
- [ ] 护栏全部报告（**未只报 OEC 提升**）
- [ ] 未改变 OEC 定义
- [ ] 未排除「不利」数据（除非事前定义排除规则）
- [ ] 不显著时说明了是哪种可能
- [ ] 事后切片标注为探索性，未直接下结论
- [ ] 按事前决策规则给出决策
