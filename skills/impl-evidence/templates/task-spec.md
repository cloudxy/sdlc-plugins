<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# AI 任务定义 · <功能名>

> 上游：PRD `<路径>` FR-<n>｜作者：/algo｜日期：<YYYY-MM-DD>
> 下游：`eval-set.md` → `model-choice.md` → `/backend`（调用契约）· `/qa`（判据）

## 1. 任务四要素

### 输入

| 项 | 内容 |
|---|---|
| 字段 | <工单标题 + 正文 + 历史对话（可选）> |
| 长度范围 | <标题 ≤ 100 字；正文 50–5000 字（P99 2800）> |
| 语言 | <中文为主，约 5% 中英混杂> |
| 来源 | <用户直接输入（**不可信，需防注入**）/ 系统数据> |
| 异常输入 | <可能为空正文、纯表情、纯链接、复制粘贴的乱码> |

### 输出

| 项 | 内容 |
|---|---|
| 结构 | <分类标签 / JSON / 自由文本> |
| 取值范围 | <`need_human` \| `auto_reply` \| `uncertain`> |
| 附加字段 | <置信度、理由（用于人工复核）> |
| 长度约束 | <理由 ≤ 50 字> |

### 怎么算对（判据）

**判据类型**：☐ 有唯一正确答案 ☐ 有多个可接受答案 ☐ 主观质量

| 判据类型 | 评估方式 |
|---|---|
| <唯一答案> | <准确率 + 召回率，可自动评> |

**主指标**：<召回率（漏判代价高，见下）>
**次指标**：<精确率 ≥ 60%（避免过度转人工）>
**目标值**：<召回 ≥ 90%，精确 ≥ 60%（eval-set-v1）>

未用自动指标伪装主观评估：☐ 确认

### 错了会怎样（失败代价）

| 错误类型 | 后果 | 代价 |
|---|---|---|
| **漏报**（该人工的没转） | <用户激怒，投诉升级> | **高** |
| 误报（不该人工的转了） | <浪费人力，约 2 分钟/条> | 低 |

**代价不对称 → 优化召回率，接受较低精确率。** 已与 `/pm` 确认：☐

### 自动化程度

| 项 | 决策 |
|---|---|
| 代价等级 | 低 / **中** / 高 |
| 自动化方式 | <自动 + 抽样人工审核（每日 20 条）+ 快速撤回能力> |
| 高代价场景的人工环节 | <N/A（本任务为中代价）/ 人工审核后才生效> |

**高代价场景（医疗/法律/财务/对外承诺）不能全自动。** 已与 `/pm` 确认：☐

## 2. NFR 约束

| 项 | 要求 | 来源 |
|---|---|---|
| P95 延迟 | <2 秒> | NFR-<n> |
| 月成本上限 | <￥3000> | `/pm` |
| 预估调用量 | <8000 次/天> | `/pm` 度量蓝图 |
| 可用性要求 | <模型不可用时降级到规则，功能不中断> | NFR-<n> |
| 数据合规 | <用户内容不得用于模型训练；不得出境> | NFR-<n> |

**数据合规是硬约束**：决定能否用外部 API。

## 3. 已知能力边界（交 /qa，避免争论）

| 场景 | 预期表现 | 是否算缺陷 |
|---|---|---|
| <真实歧义（人也难判）> | <约 12% 的样本，可能判错> | **否**，已声明边界 |
| <纯表情/乱码输入> | <返回 uncertain> | 否 |
| <超 5000 字> | <先摘要再判断，可能损失细节> | 否 |
| <多语言混杂> | <准确率下降约 8 个点> | 否 |

**这份清单把「AI 会犯错」从缺陷变成已声明的能力边界。** 没有它，`/qa` 报上来的每个错例都会引发争论。

## 4. 天花板评估

| 项 | 数字 |
|---|---|
| 人工标注一致率 | <88%> |
| 真实歧义样本占比 | <12%> |
| **可达上限估计** | <约 90%——不要承诺 95%> |

**已告知 `/pm`**：☐ 避免承诺做不到的准确率。

## 5. 与 /miner 的边界

| 我管 | `/miner` 管 |
|---|---|
| 线上推理服务（LLM/RAG/实时判断） | 离线预测建模（分群/流失/异常检测） |

本任务归属：☐ `/algo`（线上推理）

## 6. 开放问题

| 问题 | 阻塞什么 | 需要谁定 |
|---|---|---|
| <uncertain 时的产品行为：转人工还是自动回复+标记？> | 输出设计 | `/pm` |
