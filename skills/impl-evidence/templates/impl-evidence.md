<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# 实现证据 · T-<nn> <标题>

> 票：`<story 路径>`｜FR 锚点：FR-<n>｜角色：/backend｜日期：<YYYY-MM-DD>

## 1. 契约落位表（实现前填，实现后核对）

| 契约元素 | 落在哪层 | 文件 | 备注 |
|---|---|---|---|
| 路径/方法/状态码 | Router | `routers/xxx.py` | |
| 字段校验（类型/范围/枚举） | Schema | `schemas/xxx.py` | |
| 跨字段参数约束 | Schema | 同上 | 纯参数约束，不需要用户上下文 |
| 权限判定（数据范围） | **Service** | `services/xxx.py` | 需要角色信息 = 业务规则 |
| 业务规则/状态流转 | Service | 同上 | |
| 数据读写 | Repository | `repositories/xxx.py` | |
| 错误码映射 | 统一异常处理器 | `exceptions.py` + `main.py` | 不在 Router 逐个 try/except |
| 幂等 | Service + 唯一约束 | | 约束在 `<迁移文件>` |

**分层依赖核对**：☐ Router 未 import ORM ☐ Service 未返回 ORM 对象 ☐ Repository 未调 Service ☐ ORM 与 Schema 互不 import

## 2. 改动文件清单

| 文件 | 性质 | 说明 |
|---|---|---|
| `<path>` | 新增 | |
| `<path>` | 修改 | |

**与票里「会改哪些文件」一致**：☐ 是 ☐ 有偏差（说明：<...>）

**未触碰「不许改的文件」**：☐ 确认

## 3. 关键实现决策

### 事务边界

| 操作组 | 是否同事务 | 理由 |
|---|---|---|
| <创建任务 + 写 outbox> | 是 | 事件不能丢，本地消息表 |
| <入队/发通知> | 否 | 外部调用，放事务提交后 |

**事务提交后的操作失败怎么办**：<记日志+告警（可接受丢失）/ 本地消息表（不可丢）>

### 幂等

| 项 | 内容 |
|---|---|
| 幂等键来源 | <客户端 Idempotency-Key / 业务自然键 / 外部事务 ID> |
| 保证方式 | 唯一约束 `<约束名>` + 捕获重复键 |
| 重复请求返回 | <首次结果（200）/ 409> |

☐ 未使用「先查后插」

### 并发控制

| 场景 | 方式 | `rows == 0` 如何处理 |
|---|---|---|
| <状态流转> | 条件更新 `WHERE status='pending'` | <查当前状态判断是重复回调还是非法流转> |

☐ 所有条件更新的返回行数都有处理

### 外部依赖

| 依赖 | 超时 | 重试 | 降级 | 对方幂等 |
|---|---|---|---|---|
| <存储服务> | 连接 2s / 读 10s | 3 次指数退避+抖动 | <失败标记任务 failed> | 是 |

## 4. ORM 与 DBML 对齐

☐ 字段名 ☐ 类型 ☐ 可空性 ☐ 默认值 ☐ 索引 ☐ 唯一约束 ☐ 外键 —— 全部与 `schema.dbml` 一致

结构核对输出：

```
$ <SHOW CREATE TABLE 或 alembic check>
<输出>
```

**未自行加字段/改类型**：☐ 确认（需要变更已回 `/dba`）

## 5. 可观测性

| 项 | 实现 |
|---|---|
| 入口日志 | `<service 方法>` 记 <关键字段（已脱敏）> + 耗时 |
| trace_id | <从中间件生成，透传至下游与日志；5xx 响应带回> |
| 错误日志上下文 | <含业务对象 ID 与输入摘要> |
| 慢操作耗时 | <DB 查询、外部调用已记> |

**日志脱敏核对**：☐ 无密码 ☐ 无 token ☐ 无完整手机号/身份证 ☐ 无卡号 ☐ 无完整地址

## 6. 自测证据

> 命令与退出码**原样粘贴**。「测试通过」「基本完成」不算证据。

```
$ <测试命令>
<原样输出>
exit: 0

$ <架构检查>
<原样输出>
exit: 0

$ <构建>
<原样输出>
exit: 0
```

### 验收项逐条对应

| GWT | 覆盖的测试 | 结果 |
|---|---|---|
| GWT-01.1 正常 | `test_export_success` | ✅ |
| GWT-01.2 空态 | `test_export_empty` | ✅ |
| GWT-01.4 超限 | `test_export_row_limit` | ✅ |
| GWT-01.5 越权 | `test_export_forbidden_scope` | ✅ |
| GWT-07.1 脱敏 | `test_export_mask_phone` | ✅ |

### 四类易漏测试

| 类型 | 测试 | 结果 |
|---|---|---|
| 事务回滚 | `test_rollback_on_failure` | ✅ / ➖ N/A（无多步写） |
| 幂等 | `test_idempotent_create` | ✅ |
| 并发写 | `test_concurrent_conflict` | ✅ / ➖ N/A（无并发写） |
| 外部依赖失败 | `test_storage_timeout_degrade` | ✅ / ➖ N/A（无外部依赖） |

N/A 要给理由，不能空着。

## 7. NFR 验证（票里有 NFR 时填）

| NFR | 要求 | 实测 | 环境 |
|---|---|---|---|
| NFR-01 | P95 < 800ms | <实测数字> | <环境说明> |

## 8. 给下游的信息

| 给谁 | 内容 |
|---|---|
| `/qa` | <已知边界、需真库验证的方言特性、mock 了哪些依赖（真实环境需重验）> |
| `/frontend` | <实际行为与契约的差异（若有，且已回 `/architect` 确认）> |
| `/architect` | <发现的契约歧义、需要新增的错误码> |

## 9. 交票自检

- [ ] 每条验收项有 evidence（命令 + 退出码原样）
- [ ] 自测全绿（不是「大部分通过」）
- [ ] 契约落位表已核对，分层无违规
- [ ] ORM 与 DBML 一致，未自行加字段
- [ ] 无硬编码连接串/密钥/端口/阈值
- [ ] async 上下文无同步阻塞调用
- [ ] 无 `except: pass`（吞异常）
- [ ] 日志已脱敏
- [ ] 事务里无外部调用
- [ ] 幂等未用「先查后插」
- [ ] 条件更新的 `rows == 0` 已处理
- [ ] 外部依赖四件套齐全（超时/重试/降级/幂等前提）
- [ ] 四类易漏测试已覆盖或标 N/A 并给理由
- [ ] 发现的上游问题已回报，未自行绕过
- [ ] 票状态已更新为 done
