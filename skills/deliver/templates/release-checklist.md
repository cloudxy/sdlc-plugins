<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# 发布清单 · <功能名> v<版本>

> 作者：/sre｜日期：<YYYY-MM-DD>｜commit：`<sha>`
> 下游：`/qc`（放行决策依据）
> **本清单不做放行决策，只陈述检查结果。**

## 1. 变更概要与风险分级

| 项 | 内容 |
|---|---|
| 变更内容 | <新增批量导出功能；新增 export_tasks 表；新增 3 个 worker> |
| 影响面 | <工单列表页、导出接口、对象存储、异步队列> |
| **风险等级** | 低 / **中** / 高 / 不可逆 |
| 发布策略 | <按租户灰度：1 个内部租户 → 10% 租户 → 全量> |

### 三个风险问题

| 问题 | 答 |
|---|---|
| 出问题多快能发现？ | <告警 5 分钟内触发（失败率 > 5% 持续 5 分钟）> |
| 出问题多快能回滚？ | <实测 4 分 20 秒> |
| **出问题数据会不会坏？** | <否——纯新增表，不改既有数据>（会 → 升为不可逆级） |

## 2. 门禁核对（引用实际输出，不接受「应该没问题」）

| 闸门 | 命令 | 退出码 | 状态 |
|---|---|---|---|
| 单元测试 | `<命令>` | 0 | ✅ |
| 集成测试 | `<命令>` | 0 | ✅ |
| 架构检查 | `<命令>` | 0 | ✅ |
| 构建 | `<命令>` | 0 | ✅ |
| 迁移校验 | `<命令>` | 0 | ✅ |
| E2E | — | — | ➖ <未配置，理由：`sdlc.config.yaml` 声明用集成测试覆盖> |

**门禁指纹**：commit `<sha>` + 上述命令与退出码（来自 `/qa` 的 `test-report.md`）

- [ ] 所有已配置闸门全绿
- [ ] `skipped` 用例数量已解释
- [ ] 未配置的闸门有显式理由

**门禁未全绿则不发布。** 要豁免走人工流程：<责任人> + <记录位置>。

## 3. 需真实环境验证清单（来自 /qa）

| 用例 | 原因 | 执行结果 |
|---|---|---|
| <TC-030 并发行锁行为> | <SQLite 测不出> | <已在预发执行，通过> |
| <TC-031 时区转换> | <方言差异> | <已执行，通过> |
| <NFR-01 性能 P95> | <测试库数据量不足> | <预发实测 P95 = 740ms ≤ 800ms ✅> |

- [ ] 清单全部执行完毕
- [ ] 结果已回填 `/qa` 的 `test-report.md`

## 4. 回滚方案（**必须实测**）

### 代码回滚

```
$ <部署命令>                    exit: 0
$ curl -f <health>              exit: 0   （新版正常）
$ <回滚命令>                    exit: 0
$ curl -f <health>              exit: 0   （回滚后正常）
$ <一个真实业务请求>              exit: 0
```

**实测耗时**：<4 分 20 秒>

### 数据回滚

```
$ alembic upgrade +1            exit: 0
$ alembic downgrade -1          exit: 0
$ alembic upgrade +1            exit: 0
```

**数据恢复能力**：<✅ 纯新增表，down 后无数据损失> / <❌ 不可逆，已声明 + 已备份>

### 配置回滚

| 配置项 | 新值 | 旧值 | 回滚方式 |
|---|---|---|---|
| `EXPORT_ROW_LIMIT` | 10000 | — | <热更新，无需发版> |
| `EXPORT_WORKERS` | 4 | 2 | <改配置 + 重启 worker> |

### 数据兼容性（回滚的隐藏杀手）

- [ ] 新版写入的数据，旧版代码能正常读取（**已用旧版镜像验证**）
- [ ] 新增字段可空，旧版不带该字段插入不会失败
- [ ] 新增枚举值，旧版有 default 分支容忍
- [ ] 未做破坏性 DDL（或已走 expand-contract，本次仅 expand 步骤）

### 回滚窗口与限制

| 项 | 内容 |
|---|---|
| 回滚窗口 | <无限制 / 至 <时间点> 之后需人工处理> |
| **不可撤回的副作用** | <⚠️ 会向下游发出 `export.completed` 事件，已消费的不可撤回 → 回滚后需人工确认下游状态，联系 <责任人>> |
| 执行人权限 | <✅ 值班角色 <role> 已验证具备回滚权限> |

## 5. 监控接线

| 类 | 指标 | 已接 | 面板 |
|---|---|---|---|
| 可用性 | <导出接口 5xx 率> | ☐ | <链接> |
| 可用性 | <健康检查含存储依赖> | ☐ | |
| 延迟 | <导出接口 P95> | ☐ | |
| 饱和度 | <worker 队列深度> | ☐ | |
| 饱和度 | <DB 连接池使用率> | ☐ | |
| **业务** | **<导出成功率、每小时导出量>** | ☐ | |

**业务指标必须有** —— 技术指标全绿而业务量掉一半，技术监控发现不了。

### 告警（每条必须带处理指引）

| 告警 | 级别 | 阈值+持续时间 | 处理指引 |
|---|---|---|---|
| <导出失败率高> | P1 | <> 5% 持续 5 分钟> | <见 runbook 链接：3 步排查 + 临时缓解手段> |
| <worker 队列积压> | P1 | <深度 > 500 持续 10 分钟> | <扩 worker；或调低 ROW_LIMIT> |
| <存储不可用> | P0 | <上传失败率 > 50%> | <查状态页 + 检查凭证；降级为「暂不可用」提示> |

- [ ] 每条告警有可能原因 + 排查步骤 + 临时缓解 + 升级条件
- [ ] 分级合理（**不是全 P0**）
- [ ] 有抑制/聚合规则，同一问题不重复轰炸
- [ ] 阈值带持续时间（避免瞬时抖动误报）

## 6. 灰度与放量判据（提前定，不临场判断）

| 档 | 范围 | 观察时长 |
|---|---|---|
| 1 | <内部租户 1 个> | <2 小时（需覆盖一次定时任务）> |
| 2 | <10% 租户> | <1 天（需覆盖周一高峰）> |
| 3 | <全量> | <持续观察 3 天> |

### 基线（同期对比，不用发布前一分钟）

| 指标 | 基线值 | 取法 |
|---|---|---|
| 导出接口错误率 | <0.3%> | <过去 7 天同一小时均值> |
| 列表页 P95 | <820ms> | <同上> |

### 放量条件（全部满足才放）

- ✅ 错误率 ≤ 基线 + 0.5%
- ✅ P95 ≤ 基线 × 1.2
- ✅ 业务指标（导出成功率）≥ <基线 - 2%>
- ✅ 无 P0/P1 告警
- ✅ 日志无新增未知异常类型

### 回滚条件（任一满足即回滚，**不讨论**）

- ❌ 错误率 > 基线 + 2%
- ❌ P95 > 基线 × 2
- ❌ 出现数据不一致
- ❌ P0 告警
- ❌ <护栏指标：列表页 P95 > 1000ms（PRD NFR-01 红线）>

### 低频路径覆盖

| 路径 | 观察窗口是否覆盖 |
|---|---|
| <每日凌晨清理过期文件> | <☐ 档 2 观察 1 天已覆盖> |
| <缓存 TTL 后回源> | <☐ TTL 300s，已覆盖> |

## 7. 容量评估

| 项 | 数字 |
|---|---|
| 单次操作消耗 | <内存峰值 340MB，耗时 6s，占用 1 worker>（实测） |
| 预估调用量 | <8000 次/天>（来自 `/pm` 度量蓝图） |
| 峰值集中度 | <40% 集中在周一上午 2 小时> |
| 峰值并发 | <约 27 次/分钟 → 需 3 个并发 worker> |
| 内存需求 | <3 × 340MB ≈ 1GB + 基础占用> |
| **当前配置** | <2 worker / 1GB 内存 —— **不足**> |
| **调整** | <扩到 4 worker / 2GB 内存> |
| 余量 | <按峰值算留 50% 余量 ✅> |

### 瓶颈资源核对

| 资源 | 当前 | 变更后预估 | 上限 | 余量 |
|---|---|---|---|---|
| DB 连接池 | <20/50> | <30/50> | 50 | 40% |
| worker | <2> | <4> | — | — |
| 对象存储额度 | — | <+8000 次/天> | <配额> | <充足> |

## 8. 部署步骤

```
1. <确认门禁指纹与 commit 一致>
2. <应用迁移：alembic upgrade head>（可回滚，已实测）
3. <调整 worker 配置：2 → 4>
4. <部署新版本至灰度档 1>
5. <验证：健康检查 + 一次真实导出>
6. <观察 2 小时，核对放量条件>
7. <放量至档 2 → 观察 1 天>
8. <放量全量 → 观察 3 天>
```

**每步的回退动作**：

| 步 | 回退 |
|---|---|
| 2 | `alembic downgrade -1` |
| 3 | 改回配置 + 重启 |
| 4 | <回滚命令> |

## 9. 发布窗口与人员

| 项 | 内容 |
|---|---|
| 建议窗口 | <工作日上午，避开周一高峰与周五> |
| 需要维护窗口 | <否 / 是：<时长>>（来自 `/dba` 的迁移评估） |
| 值班人 | <姓名/角色> |
| 回滚决策人 | <姓名/角色> |
| 相关方通知 | <已通知客户成功团队（可能有用户咨询）> |

## 10. 检查结论（陈述事实，不做放行决策）

| 项 | 状态 |
|---|---|
| 门禁 | <全绿 / 有失败项：<列出>> |
| 需真实环境验证清单 | <全部通过 / 待执行 <n> 项> |
| 回滚方案 | <已实测，耗时 4m20s / **未实测**> |
| 数据兼容性 | <已验证 / 未验证> |
| 监控接线 | <四类齐全含业务指标 / 缺 <项>> |
| 告警 | <已配 <n> 条，均带处理指引 / 缺指引 <n> 条> |
| 灰度判据 | <已定 / 未定> |
| 容量 | <已评估，需扩容 <内容> / 未评估> |
| **阻塞发布的项** | <无 / 列出> |

**放行决策 → `/qc`。** 我提供检查结果，不做放行判断（铁律：产出者不批准自己的工件）。

## 11. 自检

- [ ] 风险已分级，策略与之匹配
- [ ] 三个风险问题已回答
- [ ] 门禁引用实际输出（命令 + 退出码），未接受口头声称
- [ ] 需真实环境验证清单已执行并回填
- [ ] **三类回滚全部实测**（代码/数据/配置）
- [ ] 回滚耗时是实测数字
- [ ] 数据兼容性已用旧版验证
- [ ] 不可撤回的副作用已列出
- [ ] 执行人权限已验证
- [ ] 监控四类齐全，**含业务指标**
- [ ] 每条告警有处理指引，分级合理
- [ ] 放量与回滚判据提前写定
- [ ] 基线用同期数据
- [ ] 观察时长覆盖低频路径
- [ ] 容量按峰值评估，留 50% 余量
- [ ] 每个部署步骤有回退动作
- [ ] 未做放行决策
