# 故障报告 · INC-<nnn> <一句话现象>

> 作者：/sre｜日期：<YYYY-MM-DD>
> **对事不对人。** 追问「流程为什么允许这个错误发生」，不追问「谁写错了」。

## 1. 概要

| 项 | 内容 |
|---|---|
| 严重度 | **P0** / P1 / P2 |
| 影响功能 | <导出功能> |
| 影响范围 | <约 <n> 个用户 / <x>% 请求 / 仅 <某租户>> |
| 影响时长 | <21 分钟>（首次影响到完全恢复） |
| **发现耗时** | <9 分钟>（问题发生到告警/人工发现） |
| **恢复耗时** | <12 分钟>（确认到恢复） |
| 数据影响 | <无 / 有：<n> 条记录需修复> |
| 止损手段 | <回滚 / 降级 / 限流 / 扩容> |

**发现耗时与恢复耗时是改进监控与响应流程的核心依据。**

## 2. 时线（恢复后立即记，不要等复盘）

| 时间 | 事件 |
|---|---|
| 14:32 | <发布 v2.3.1（灰度 10%）> |
| 14:38 | <首个失败请求（事后从日志确认）> |
| 14:41 | <告警触发 P1：导出失败率 8.2%> |
| 14:43 | <值班确认，开始排查> |
| 14:47 | <定位：失败集中在存储上传步骤> |
| 14:49 | <决定回滚> |
| 14:53 | <回滚完成，失败率回落至 0.3%> |
| 14:59 | <确认恢复正常> |
| 15:20 | <根因确认> |

**时线要在记忆新鲜时记。** 隔一天回忆，时间点与判断过程都会失真。

## 3. 用户影响

| 项 | 内容 |
|---|---|
| 用户看到什么 | <点击导出后提示「导出失败，请稍后重试」> |
| 有 workaround 吗 | <无 / 有：<内容>> |
| 收到用户报障 | <n> 条（<早于/晚于> 告警） |
| 数据是否受损 | <否 / 是：<n> 条任务状态卡在 processing，已批量修正> |
| 需要用户做什么 | <重新发起导出 / 无需操作> |
| 已通知用户 | <是（站内公告 / 客户成功团队）/ 否> |

**「用户报障早于告警」是重要信号** —— 说明监控不够灵敏，要进改进项。

## 4. 直接原因与根本原因

### 直接原因

<存储 SDK 从 2.1 升到 3.0，凭证格式从 access_key 改为 token，配置未同步更新。>

### 根本原因（追问「为什么能走到生产」）

| # | 根本原因 |
|---|---|
| ① | <依赖升级未在预发环境验证存储上传路径> |
| ② | <预发环境用 mock 存储，与生产不一致（环境保真度不足）> |
| ③ | <集成测试也 mock 了存储，未覆盖真实上传> |

**只找直接原因会导致同类问题反复发生。**

## 5. 逃逸归因

**先分类**（决定处理路径）：

| 类型 | 本次 |
|---|---|
| 需求/实现缺陷（逻辑错、边界漏） | ☐ → 走全链回流 |
| **环境/流程逃逸**（CI/部署/依赖/证书/环境不一致） | ☑ → **走逃逸归因，改流程不改需求** |

### 四问

| 问 | 答 |
|---|---|
| ① 本该在哪道闸门被拦住？ | <集成测试 / 预发验证> |
| ② 为什么没拦住？ | <两处都 mock 了存储，不走真实上传路径> |
| ③ 怎么改闸门？ | <预发接真实存储沙箱；集成测试加真实上传用例，依赖变更时触发> |
| ④ 会让闸门太慢或太吵吗？ | <真实上传测试约 3 秒；只在依赖变更时跑，不进每次 CI> |

**「加个人工检查项」通常是坏答案** —— 人会忘。能自动化的要自动化。

## 6. 改进项（必须有责任人和时间）

| # | 改进项 | 类型 | 责任人 | 时间 | 状态 |
|---|---|---|---|---|---|
| 1 | <预发环境接真实存储沙箱> | 环境 | <人> | <日期> | ☐ |
| 2 | <集成测试加真实上传用例（依赖变更触发）> | 闸门 | <人> | <日期> | ☑ 已完成 |
| 3 | <依赖升级流程加「影响的外部集成清单」步骤> | 流程 | <人> | <日期> | ☐ |
| 4 | <导出失败率告警阈值从 5% 降到 3%，持续时间从 5 分钟降到 3 分钟> | 监控 | <人> | <日期> | ☐ |
| 5 | <加存储凭证有效性的启动时校验（快速失败）> | 实现 | `/backend` | <日期> | ☐ |

**无责任人和时间的改进项等于没有。**

### 不可自动化的部分

| 项 | 缓解 |
|---|---|
| <依赖升级时需人工判断影响哪些外部集成> | <已加入 PR 模板检查项> |

## 7. 监控与响应的复盘

| 问题 | 答 | 改进 |
|---|---|---|
| 告警是否及时？ | <9 分钟发现，可以更快> | 改进项 4 |
| 告警指引是否有用？ | <有用，第 2 步直接指向了存储> | — |
| 是否有误报干扰？ | <无> | — |
| 回滚是否顺利？ | <是，4 分钟，与实测一致> | — |
| 决策是否犹豫？ | <14:47 定位到 14:49 决定回滚，2 分钟，可接受> | — |
| 有无先查因后止损？ | <无，先回滚了> ✅ | — |

**「先止损再查因」执行情况**：☑ 正确 / ☐ 违反（说明：<...>）

## 8. 相关

| 项 | 内容 |
|---|---|
| 相关发布 | <v2.3.1，commit `<sha>`> |
| 相关 ADR | <ADR-00n 存储选型> |
| 相关故障 | <INC-0nn（同类根因）> |
| 已记入 | `_lessons.md` ☐ |

**同类根因反复出现时**，说明是系统性问题——不要只修单次，要改机制。

## 9. 自检

- [ ] 时线在恢复后立即记录（非事后回忆）
- [ ] 发现耗时与恢复耗时已算出
- [ ] 用户影响已评估，含是否早于告警收到报障
- [ ] 数据影响已确认，受损数据已修复
- [ ] 区分了直接原因与根本原因
- [ ] 已分类（需求缺陷 vs 环境逃逸），走了正确路径
- [ ] 逃逸归因四问已答
- [ ] 每个改进项有责任人和时间
- [ ] 不可自动化的部分已标注缓解手段
- [ ] 监控与响应流程本身已复盘
- [ ] **全文对事不对人**，无追责表述
- [ ] 已记入 `_lessons.md`
