<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# 迁移自检 · <migration_id> <一句话变更内容>

> 上游：`db-spec.md` / `schema.dbml`｜变更类型：☐ 纯加法 ☐ 破坏性（走 expand-contract 第 <n>/3 步）

## 1. 变更清单

| 对象 | 操作 | 破坏性 |
|---|---|---|
| `<table>.<column>` | ADD COLUMN (nullable) | 否 |
| `<table>` | ADD INDEX `idx_xxx` | 否 |

破坏性变更必须已拆成三步，且本迁移只含其中**一步**。三步写进一个文件 = 不通过。

## 2. 可逆性验证（必跑，不是必写）

```
$ <up 命令>              # 如 alembic upgrade +1
exit: 0
$ <down 命令>            # 如 alembic downgrade -1
exit: 0
$ <up 命令>
exit: 0
```

粘贴真实输出，退出码原样。三段跑通才算可回滚。

**`down` 能恢复数据吗？**
- ☐ 能（纯结构变更，或 down 里有数据恢复语句）
- ☐ 不能——原因：<...>。已声明「此步不可逆」并要求交付前备份。

## 3. 结构验证

```
$ <验证命令>   # 如 mysql -e "SHOW CREATE TABLE orders\G"
<输出，确认新结构与 schema.dbml 一致>
```

对齐检查：字段名 / 类型 / 可空性 / 索引名 / 索引列顺序 与 `schema.dbml` 逐项一致（下游 ORM 按 DBML 建模，不一致就会在 `/backend` 那边炸）。

## 4. 锁与耗时评估（大表必填）

| 项 | 值 |
|---|---|
| 目标表当前行数 | <n> |
| 在线 DDL 算法 | `ALGORITHM=INPLACE, LOCK=NONE` / `COPY`（会锁写） |
| 预计耗时 | <n> 秒（测试库实测 or 按行数估算） |
| 需要维护窗口 | 是 / 否 |

`COPY` 算法 + 百万行以上 = 需要窗口或 gh-ost/pt-osc 之类的在线改表工具。写清楚，别让 `/sre` 在发布时才发现。

## 5. 回填（无回填则写 N/A）

| 项 | 内容 |
|---|---|
| 回填脚本 | `<路径>`（独立可重跑脚本，不写在迁移里） |
| 批大小 | <n> 行/批，批间 sleep <n>ms |
| 幂等守卫 | `WHERE <目标列> IS NULL` |
| 可续跑 | 按 `id` 区间推进，中断可续 |
| 完成校验 | `SELECT COUNT(*) FROM ... WHERE <未回填条件>;` → 必须 0 |

```
$ <回填命令>
exit: 0
$ <校验 SQL>
0
```

## 6. EXPLAIN 证据（新增/变更索引时必填）

```
-- <模式 ID>：<场景>
<SQL>
<EXPLAIN 输出，原样，不截列>
判读：type=? key=? rows=? Extra=?  → 通过 / 需优化
```

每个受影响的 Top-N 模式一段。`type: ALL` 或意外 `Using filesort` / `Using temporary` 未说明 = 不通过。

## 7. 自检清单

- [ ] 变更与 `schema.dbml` 一致（字段名/类型/可空/索引名/列顺序）
- [ ] 破坏性变更已拆三步，本文件只含一步
- [ ] `up → down → up` 实跑通过，退出码贴出
- [ ] `down` 的数据恢复能力已明确（能 / 不能+已声明）
- [ ] 大表锁行为与耗时已评估
- [ ] 回填脚本分批、幂等、可续跑、有归零校验
- [ ] 新增索引有 EXPLAIN 证据
- [ ] 无硬编码连接串 / 密钥
- [ ] 迁移文件里无大回填（DDL 与数据操作分离）
- [ ] 方言特性已标「需真库验证」

## 8. 交付给下游

| 给谁 | 内容 |
|---|---|
| `/backend` | 最终结构（DBML）+ 枚举合法值 + 新字段的业务语义 |
| `/qa` | 需真库验证的清单 + 回滚验证步骤 |
| `/sre` | 是否需要维护窗口 + 预计耗时 + 回滚命令 |
