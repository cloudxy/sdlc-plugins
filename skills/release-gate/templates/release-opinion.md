<!-- sdlc:unfilled — 写入本次的真实内容后删除本行；带这一行的文件在闸门里等于没交 -->
# 放行意见 · <功能名> v<版本>

> 作者：/qc｜日期：<YYYY-MM-DD>｜决策对象 commit：`<sha>`
> **结论必须是三者之一：放行 / 有条件放行 / 不放行。** 不允许「基本可以」「风险可控」这类模糊表述。

## 1. 结论

| 项 | 内容 |
|---|---|
| **结论** | ☐ 放行　☐ 有条件放行　☐ **不放行** |
| 一句话理由 | <...> |
| 阻塞项数量 | <n> |

### 有条件放行的条件（若适用，必须具体可执行）

| # | 条件 | 责任人 | 验证方式 |
|---|---|---|---|
| 1 | <限灰度 10%，观察 24 小时后再放量> | `/sre` | <放量判据见 release-checklist 第 6 节> |
| 2 | <`/pm` 书面确认剩余风险 R-02> | `/pm` | <回复记录> |

### 不放行时的最小修复集（若适用）

| # | 必须修什么 | 为什么阻塞 | 指派 |
|---|---|---|---|
| 1 | <FR-08 完全未被覆盖矩阵包含> | <该需求无人测试且无人察觉> | `/qa` |
| 2 | <blocker QA-02 仍 open> | <跨租户数据泄露> | `/backend` |

**最小修复集 = 只列真正阻塞的**，不夹带「顺便改一下」的建议。

## 2. 门禁指纹

**先确认审的版本与发布的版本一致。**

| 项 | 值 |
|---|---|
| 决策对象 commit | `<sha>` |
| 各闸门执行的 commit | `<sha>`（☐ 一致 / ☐ **不一致 → 阻塞**） |
| 有无「测完之后又改代码」 | ☐ 无　☐ 有（☐ 已重跑闸门 / ☐ **未重跑 → 阻塞**） |

| 闸门 | 命令 | 退出码 | 通过/失败/跳过 | 状态 |
|---|---|---|---|---|
| 单元测试 | `<命令>` | 0 | <n>/<0>/<n> | ✅ |
| 集成测试 | `<命令>` | 0 | <n>/<0>/<0> | ✅ |
| 架构检查 | `<命令>` | 0 | 0 违规 | ✅ |
| 构建 | `<命令>` | 0 | — | ✅ |
| 迁移校验 | `up→down→up` | 0/0/0 | — | ✅ |
| E2E | — | — | — | ➖ <未配置，理由：`sdlc.config.yaml` 的 `e2e_reason`> |

- [ ] 全部为原样输出（命令 + 退出码），非「测试都过了」的声称
- [ ] `skipped` 数量已逐条解释
- [ ] 未配置的闸门有显式理由

**我不改判确定性门禁。** 任何 exit != 0 都是阻塞；要豁免走人工流程（下方第 6 节记录）。

## 3. 覆盖完整性核对（六类）

> **这是 `/qc` 的独特价值：证明没有漏掉的维度。** 不重做 `/reviewer` 的逐维审查。

### ① FR/NFR 完整性（清单对清单，机械核对）

| 项 | 数量 | 明细 |
|---|---|---|
| PRD 的 FR 清单 | <8> | FR-01…FR-08 |
| 覆盖矩阵出现的 FR | <7> | FR-01…FR-07 |
| **缺失** | **<1>** | **FR-08 → 阻塞项**（谁都没测，且谁都没察觉） |
| PRD 的 NFR 清单 | <10> | NFR-01…NFR-10 |
| 矩阵出现 / N/A | <7> / <3> | |
| 缺失 | <0> | |

### ② 空洞声明完整性

| 空洞 | 类型 | 有理由 | 有处置 | 判定 |
|---|---|---|---|---|
| GWT-01.6 部分失败 | ❌ 缺口 | ✅ | ✅ 已提 BUG-012 | 见剩余风险 R-01 |
| NFR-01 性能 | ⚠️ 需真实环境 | ✅ | ✅ 已由 `/sre` 执行 | 已闭环 |
| NFR-09 国际化 | ➖ N/A | ✅ PRD 声明 | — | 可接受 |

- [ ] 无「空着未标注」的格子（空着 = 无法区分「漏了」与「不需要」）

### ③ 角色参与完整性

| 角色 | solution 声明 | 实际交付工件 | 判定 |
|---|---|---|---|
| `pm` | 参与 | `01-define/spec.md` | ✅ |
| `architect` | 参与 | `02-shape/contract.md` `adr-005` | ✅ |
| `dba` | 参与 | `02-shape/db-spec.md` `02-shape/schema.dbml` | ✅ |
| `designer` | 参与 | **缺 `02-shape/edge-states.md`** | ❌ **阻塞** |
| `frontend` | 参与 | 实现 + 九维自查证据 | ✅ |
| `backend` | 参与 | 实现 + evidence | ✅ |
| `qa` | 参与 | `04-verify/coverage.md` | ✅ |
| `sre` | 参与 | `06-deliver/checklist.md` | ✅ |
| `algo` `miner` | **N/A** | — | ✅ 理由成立（无 AI/建模需求） |

**「有界面但无 `edge-states.md`」是典型漏洞**：说明设计环节被跳过，六态大概率是 `/frontend` 自己编的。

### ④ 闸门配置完整性

- [ ] `sdlc.config.yaml` 配置的闸门都跑了
- [ ] 未配置的闸门有显式理由（如 `e2e_reason`）
- [ ] 无「配了但被跳过」的闸门

### ⑤ 跨工件一致性抽查（抽查，不重做全量）

| 项 | 抽查内容 | 结果 |
|---|---|---|
| 接口名与字段 | <契约 ↔ 后端 ↔ 前端，抽 3 处> | ✅ |
| 表名与字段 | <DBML ↔ ORM ↔ 迁移，抽 2 处> | ✅ |
| 状态枚举值 | <PRD 流转图 ↔ db-spec ↔ 代码常量 ↔ 用例> | ✅ |
| 错误码 | <契约 ↔ 后端抛出 ↔ 前端分支（含 default）> | ⚠️ <契约有 429，前端无对应分支 → minor> |
| 事件名与字段 | <事件契约 ↔ 生产方 ↔ 消费方 ↔ 埋点 FR> | ✅ |

### ⑥ 关键旅程与验收结论

| 项 | 证据 | 结果 |
|---|---|---|
| 每条 J-n 有 E2E 且已运行 | `coverage.md` §1b · state `gates[] e2e` | ✅ / ❌ |
| PM 验收走查 | `04-verify/accept-pm.md` 结论：<> | |
| 设计走查（有界面） | `04-verify/accept-design.md` 结论：<> | |
| 卖点核验（growth 未跳过） | `04-verify/accept-growth.md` 结论：<> | |

**任一结论为「不通过」→ 不放行。**「有条件通过」的条件必须已被人接受并有负责人。

## 4. findings 处置状态

> 每条必须是 fixed / waived（带理由+批准人）/ open 之一。**不允许悬空。**

| 编号 | 严重度 | 一句话 | 状态 | 修复 commit / waive 理由 + 批准人 |
|---|---|---|---|---|
| QA-01 | blocker | <空心用例 `assert True`> | **fixed** | `<sha>` + 已重跑 TC-004 通过 |
| QA-02 | blocker | <查询未过滤 tenant_id> | **fixed** | `<sha>` + 已补 TC-214 跨租户用例 |
| QA-03 | major | <契约 429 前端无分支> | **waived** | <本轮限流未启用；理由：<...>；批准人：`/architect` <名>> |
| QA-04 | minor | <命名不一致> | waived | <记入技术债 <位置>> |

### 汇总

| 严重度 | 总数 | fixed | waived | **open** |
|---|---|---|---|---|
| blocker | <2> | <2> | **0** | **<0>** |
| major | <1> | <0> | <1> | <0> |
| minor | <1> | <0> | <1> | <0> |

**放行判据核对**：

- [ ] blocker 全部 fixed（**不接受 waived** —— 能 waive 说明定级错了，要求重新定级）
- [ ] major 全部 fixed 或有理由+批准人的 waived
- [ ] 无「无理由的 waived」（伪处置：「不影响主流程」「下个版本修」而无排期 = open）
- [ ] 所有 open 项已列为阻塞

### 修复引入的新风险

| 项 | 状态 |
|---|---|
| 修复后是否改了代码 | ☐ 是（改了 <n> 处） |
| **闸门是否重跑** | ☐ 已重跑，指纹已更新为 `<sha>`　☐ **未重跑 → 阻塞** |

**修复之后必须重跑闸门** —— 这是最常被跳过的一步，而修复本身可能引入新问题。

## 5. 剩余风险

> 放行不等于零风险，等于**剩余风险已知且可接受**。每项四要素。

| # | 风险描述 | 可能后果 | **能否被及时发现**（有无监控） | 恢复成本 | 判定 |
|---|---|---|---|---|---|
| R-01 | <GWT-01.6 部分失败分支未验证> | <行数不符且无提示> | <⚠️ 无专项监控，靠用户报障> | <低（前端文案修复）> | **需 `/pm` 确认** |
| R-02 | <限流场景前端无分支（QA-03 waived）> | <显示「未知错误」> | <✅ 前端错误上报有监控> | <低> | 可接受 |
| R-03 | <首次使用真实存储沙箱> | <上传失败> | <✅ 失败率告警 P1，5 分钟> | <低（回滚 4m20s）> | 可接受 |

### 判定框架对照

| 情形 | 判定 | 本次是否命中 |
|---|---|---|
| 有监控 + 能快速回滚 + 无数据损坏 | 可接受 | R-02 R-03 |
| **无监控**（出问题也不知道） | **不可接受，要求补监控** | <R-01 → 已要求补埋点或降为有条件放行> |
| 回滚窗口已过 / 回滚有损 | 需业务方明确接受 | <无> |
| 可能数据损坏 | 不可接受（除非备份+演练） | <无> |
| 空洞在核心路径 | 不可接受，要求补测 | <无> |
| 空洞在边缘路径 + 有监控 | 可接受并记录 | <R-01 属边缘> |

### 上抛项（超出 `/qc` 判断范围，**不替他们决定**）

| 风险 | 上抛给谁 | 需要什么决定 | 状态 |
|---|---|---|---|
| R-01 | `/pm` | <批量部分失败无提示，业务能否接受一个版本？> | ☐ 待回复 |
| <架构层面> | `/architect` | <...> | — |

## 6. 人工豁免记录（若有门禁失败仍需发布）

> **我无权改判确定性门禁。** 门禁失败要发布只能走此流程。

| 项 | 内容 |
|---|---|
| 失败的闸门 | <命令 + 退出码> |
| 豁免理由 | <...> |
| **批准人** | <姓名/角色>（**必须是能承担后果的人，不是 `/qc`**） |
| 记录位置 | <链接> |
| 补救计划 | <什么时候修 + 责任人> |

无豁免时写「本次无人工豁免」。

## 7. 我做了什么 / 没做什么

| | |
|---|---|
| **我做的** | 收集闸门证据、核对门禁指纹、核对五类覆盖完整性、核对 findings 处置、判定剩余风险、给出放行结论 |
| **我没做的** | 重做 `/reviewer` 的逐八维审查（他已做，我只抽查一致性）· 修代码 · 改判门禁 · 替 `/pm` 决定业务风险 |
| 我是否为本轮工件的产出者 | ☐ **否**（铁律：产出者永不批准自己的工件） |

## 8. 自检

- [ ] 结论是三者之一，无「基本可以」「风险可控」等模糊表述
- [ ] 门禁指纹完整，commit 一致，无未重跑的改动
- [ ] 全部闸门证据为原样输出，非声称
- [ ] 五类覆盖完整性逐项核对（FR 清单对清单是机械核对）
- [ ] 角色参与完整性已对照 solution 声明
- [ ] findings 每条状态明确，blocker 无 waived，无无理由 waived
- [ ] 修复后闸门已重跑
- [ ] 剩余风险每项四要素，含「能否被及时发现」
- [ ] 无监控的风险未被判为可接受
- [ ] 超出范围的风险已上抛，未替他人决定
- [ ] 有条件放行的条件具体可执行
- [ ] 不放行时给了最小修复集，无夹带建议
- [ ] 未改判任何确定性门禁
- [ ] 未修改任何代码
- [ ] 确认自己不是本轮产出者
